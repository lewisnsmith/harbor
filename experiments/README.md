# Experiments

Temporary or exploratory scripts and prototypes live here. Promote stable work into `harbor/` or `notebooks/`.

Current runnable baselines:
- `h1_end_to_end_hrp_backtest.py` — Phase H1 exit-criteria pipeline (`load -> HRP -> backtest -> risk metrics`).
